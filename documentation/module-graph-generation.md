# Module graph generation

In order to generate a module graph of the project execute the following command:

`./gradlew createModuleGraph`

The generated graph will be saved to a markdown file [module-graph.md](module-graph.md) in a mermaid format.